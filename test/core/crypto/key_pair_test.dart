library nem2_sdk_dart.test.core.crypto.key_pair_test;

import "package:test/test.dart";
import "package:nem2_sdk_dart/src/core/crypto.dart";

void main() {
  test("create new key pair", () {
    final KeyPair kp = new KeyPair();

    // expect(kp.hasPrivateKey(), true);
  });
}